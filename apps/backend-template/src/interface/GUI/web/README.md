# Web GUI placeholders

Future home for web GUIs that drive the backend-template application:

- SPA / PWA clients (React, Vue, Svelte, …)
- Full sites and docs shells that consume Jumentix contracts
- Offline-capable frontends that still call inbound adapters for server work

No implementations yet. Keep UI frameworks out of `domain/` and
`application/`.
