# Requirement 099 - Pre-task Branch and Requirement Refresh

Before planning or executing every task, every agent must fetch and read current `main` and `dev`, inspect every file in `.agents/requirements/` for additions or changes, and read the NFR registry and specification index. A prior task refresh is not evidence for a later task. Execution is blocked until the refresh is complete and registry branch-check evidence is updated.
