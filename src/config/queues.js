'use strict'

export default {
  JumentiX: {
    // JumentiX.Notification
    ApplicationStats: {
      description: 'Messaging covering APIs and Workers stats'
    },
    Mediator: {
      description: 'Send messages to clients connected to JumentiX via Mediator'
    },
    Gmailer: {
      description: 'Send e-mail messages to clients via Gmail'
    }
  }
}
