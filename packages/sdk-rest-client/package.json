{
  "name": "@jumentix/sdk-rest-client",
  "version": "0.0.0",
  "private": true,
  "description": "Jumentix REST SDK client",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "bun test --isolate test",
    "lint": "echo \"sdk-rest-client: lint managed by root workspace\"",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "yaml": "^2.5.1"
  }
}
