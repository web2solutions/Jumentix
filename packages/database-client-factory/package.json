{
  "name": "@jumentix/database-client-factory",
  "version": "0.1.0",
  "private": true,
  "description": "Database client compiler factory for JumentiX runtimes.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint ./src --ext .ts",
    "test": "echo \"No tests yet for @jumentix/database-client-factory\""
  }
}
