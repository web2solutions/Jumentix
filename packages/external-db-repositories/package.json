{
  "name": "@jumentix/external-db-repositories",
  "version": "0.1.0",
  "private": true,
  "description": "Reusable external database repository adapters for JumentiX.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint ./src --ext .ts",
    "test": "echo \"No tests yet for @jumentix/external-db-repositories\""
  }
}
