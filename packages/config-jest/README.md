# @jumentix/config-jest

Private workspace placeholder for the future shared Jest configuration.

## Current `dev` status

- Private and not publishable.
- Exposes `index.cjs` through the package file list.
- All lifecycle scripts explicitly report that the migration wave is pending.

The repository still uses its root Jest configuration and CI runners as the executable source of
truth.
