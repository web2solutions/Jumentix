# @jumentix/persistence-contracts

Reusable persistence contracts for Jumentix services and adapters.

## Exports

- `IStore` and advanced query/mutation/index contracts
- `IDatabaseClient<TStores>` generic client contract
- `IPagingRequest` and `IPagingResponse` transport-friendly paging contracts
