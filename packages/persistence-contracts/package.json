{
  "name": "@jumentix/persistence-contracts",
  "version": "0.1.0",
  "private": true,
  "description": "Shared persistence contracts for JumentiX applications and adapters.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint ./src --ext .ts",
    "test": "bun run typecheck"
  },
  "jumentix": {
    "testSurface": "typecheck-only"
  }
}
