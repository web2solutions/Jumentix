{
  "name": "@jumentix/shared-contracts",
  "version": "0.1.0",
  "private": true,
  "description": "Shared OpenAPI/AsyncAPI contract resolution helpers for the JumentiX SDKs.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint ./src --ext .ts",
    "test": "bun test --isolate test"
  },
  "dependencies": {
    "yaml": "^2.5.1"
  }
}
