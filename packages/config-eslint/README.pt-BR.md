# @jumentix/config-eslint

Placeholder privado do workspace para a futura configuração ESLint compartilhada.

## Estado atual em `dev`

- Privado e não publicável.
- Expõe `index.cjs` pela lista de arquivos do pacote.
- `build`, `test`, `lint` e `typecheck` são placeholders explícitos de migração pendente.

Não dependa deste pacote como configuração concluída até que sua tarefa de migração substitua os
scripts placeholder por validação executável.
