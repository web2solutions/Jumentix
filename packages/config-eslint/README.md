# @jumentix/config-eslint

Private workspace placeholder for the future shared ESLint configuration.

## Current `dev` status

- Private and not publishable.
- Exposes `index.cjs` through the package file list.
- `build`, `test`, `lint`, and `typecheck` are explicit migration-pending placeholders.

Do not depend on this package as a completed configuration until its migration task replaces the
placeholder scripts with executable validation.
